# Next Steps

## Immediate Replacement Checklist

- Replace team member placeholders with real names and profile content.
- Add real headshots or approved portrait placeholders.
- Replace Gantt placeholder tasks with the final project management data.
- Replace interactome placeholder nodes and edges with the real network.
- Insert the three professionals' actual interview database content.
- Replace interview summary placeholders with final summaries.
- Add proof of professional behavior evidence.

## Future Polish Tasks

- Run `npm run qa:visual` after major visual edits.
- Decide whether to remove the no-build static preview after the React app is fully adopted.
- Add responsive screenshots after real content insertion.
- Tune section heights once all content is known.
- Add accessible labels to final logo images.
- Consider a print/PDF-friendly export mode for evaluation backup.
- Add lightweight active-section highlighting in the navigation panel.
- Add optional filtering/search for the interview database if content becomes dense.

## Current Status

The prototype is in first-build status. Local Node/npm and Playwright are now installed, `npm run build` passes, and a Vite dev server can run the React prototype.
